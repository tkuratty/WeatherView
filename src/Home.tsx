import React from "react";
import { Pref } from "./Utils/Area";

const Home: React.FC<{ area: Array<Pref> }> = (props) => {
  return (
    <div>
      <p>Home</p>
    </div>
  );
};

export default Home;
